import React from 'react';

const FileManager = () => {
  return (
    <div className="file-manager">
      <p>파일: <span>비어 있음</span></p>
      <button>속성 추가</button>
    </div>
  );
};

export default FileManager;
